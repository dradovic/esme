@page "/home"
@inherits HomeBase

@if (Circles == null)
{
    <p>Loading...</p>
}
else
{
    @foreach (var circle in Circles)
    {
        <div>
            <button type="button" class="btn btn-link ml-md-auto" onclick="@(() => CircleClick(circle))">@circle.Name</button>
        </div>
    }
}
